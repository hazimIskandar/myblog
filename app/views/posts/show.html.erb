<div class="container">
	<h1><%= @post.title %></h1>
	<p><%= @post.content %></p>
	<p><%= @post.created_at%></p>
	<%= link_to "delete", @post, method: :delete,
		data: { confirm: "You sure?" } %>
	<%= link_to "edit", edit_post_path%>
</div>

